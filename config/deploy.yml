# =============================================================================
# Deployment Configuration
# =============================================================================
# Non-secret values for the CI/CD pipeline. Edit these to match your AWS setup.
# This file is committed to git — never put passwords or keys here.
# =============================================================================

aws_region: us-east-2
aws_account_id: "199865934287"
ecr_repository: todo-app2
ecs_cluster: default
ecs_service: todo-app-ec13
